# `POST /user/uploadKey`

Creates a new upload key

## Headers
- `Authorization: Bearer aaaa...` - the user's jwt token

## Returns (JSON)
```json
{
	"success": true,
	"key": "aaaa..."    // the upload key
}
```
